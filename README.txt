to run this:

run xampp and start both apache and mysql

create a database named 'gadgets'
on the just created database go to import tab

click browse and select gadgets.sql file

hit Go

on the url type in localhost/Gadgets/